name: Build

on:
  push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: checkout
      uses: actions/checkout@v2
    - name: coursier-setup
      uses: laughedelic/coursier-setup@v1
      with:
        jvm: adopt:11
        apps: sbtn
    - name: Run tests
      run: sbtn test
